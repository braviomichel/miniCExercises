#include<stdio.h>

int MIN (int a, int b)
{
	if(a<b)
		return a;
	else
		return b;
}

int MAX (int a, int b)
{
	if(a>b)
		return a;
	else
		return b;
}
 int main ()
 {
 	int a,b,c,d,Maximum, minimum;
 	printf("Entrez les quatres nombres\n");
 	scanf("%d,%d,%d,%d",&a,&b,&c,&d);
 	
 	printf("le MAximum des 4 nombres est %d\n ", Maximum;
 	printf("le Minimum des 4 nombres est %d\n",minimum;
return 0;
 }
 
